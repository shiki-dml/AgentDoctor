#:schema https://developers.openai.com/codex/config-schema.json
# Project-scoped Codex configuration for Contract2Agent.
#
# Source of truth:
# - AGENTS.md
# - docs/README.md
# - docs/PROJECT_CONTEXT.md
# - docs/ARCHITECTURE.md
# - docs/CODEMAP.md
# - docs/AGENT_HANDOFF.md
# - docs/harness/PROGRESS.md
# - docs/harness/feature_registry.json
#
# This file follows the "Everything Claude Code" pattern of making agents,
# skills, and MCP candidates explicit, but keeps networked MCP servers disabled
# until a user authorizes package downloads, credentials, and network access.

approval_policy = "on-request"
sandbox_mode = "workspace-write"
include_permissions_instructions = true
include_environment_context = true
include_apps_instructions = true
project_doc_max_bytes = 32768
project_doc_fallback_filenames = ["AGENTS.md"]
project_root_markers = [".git", "AGENTS.md", "pyproject.toml"]

[features]
multi_agent = true
skill_env_var_dependency_prompt = true
skill_mcp_dependency_install = false

[skills]
include_instructions = true

[skills.bundled]
enabled = true

[agents]
max_threads = 4
max_depth = 1
# Lean Reflexion-inspired active role set:
# mapper gathers context, contract_generator scopes the episode,
# feature_generator acts, evaluator/bug_reviewer supply feedback,
# and handoff_writer records repository-backed verbal memory.

[agents.codebase_mapper]
description = "Read-only repository mapping and ownership discovery for Contract2Agent."
config_file = "agents/codebase-mapper.toml"

[agents.contract_generator]
description = "Episode scope writer for approved/proposed sprint contracts only."
config_file = "agents/contract-generator.toml"

[agents.feature_generator]
description = "Actor role for approved-contract-scoped implementation."
config_file = "agents/feature-generator.toml"

[agents.handoff_writer]
description = "Repository-backed verbal memory writer for handoff and progress artifacts."
config_file = "agents/handoff-writer.toml"

[agents.bug_reviewer]
description = "Read-only correctness and regression-risk feedback provider."
config_file = "agents/bug-reviewer.toml"

[agents.evaluator]
description = "Read-only evaluator for PASS / FAIL / INCONCLUSIVE / BLOCKED feedback."
config_file = "agents/evaluator.toml"

[mcp_servers.context7]
enabled = false
required = false
command = "npx"
args = ["-y", "@upstash/context7-mcp@latest"]
startup_timeout_sec = 30
tool_timeout_sec = 60

[mcp_servers.github]
enabled = false
required = false
command = "npx"
args = ["-y", "@modelcontextprotocol/server-github@latest"]
env_vars = ["GITHUB_PERSONAL_ACCESS_TOKEN"]
startup_timeout_sec = 30
tool_timeout_sec = 60

[mcp_servers.playwright]
enabled = false
required = false
command = "npx"
args = ["-y", "@playwright/mcp@latest"]
startup_timeout_sec = 30
tool_timeout_sec = 60

[mcp_servers.sequential_thinking]
enabled = false
required = false
command = "npx"
args = ["-y", "@modelcontextprotocol/server-sequential-thinking@latest"]
startup_timeout_sec = 30
tool_timeout_sec = 60

[mcp_servers.memory]
enabled = false
required = false
command = "npx"
args = ["-y", "@modelcontextprotocol/server-memory@latest"]
startup_timeout_sec = 30
tool_timeout_sec = 60
